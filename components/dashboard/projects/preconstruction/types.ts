// Re-export shared project types so existing imports keep working.
export {
	type Material,
	type MaterialStatus,
	type EsgTarget,
  type SupplierApprovalStatus,
	units,
	initialMaterials,
} from "@/types/project";
